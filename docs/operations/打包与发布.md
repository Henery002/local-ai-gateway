# 打包与发布说明

本文档描述 `Local AI Gateway` 当前的桌面打包与发布脚手架。

## 1. 当前目标

当前阶段的打包能力定位为：

- 先具备本地构建桌面包的基础能力
- 为后续发布流程预留统一脚手架
- 暂不追求完善的签名、公证与自动发布

当前已验证：

- `npm run package:desktop` 可生成目录包
- 产物位置为 `release/mac-arm64/Local AI Gateway.app`
- `npm run smoke:desktop-package` 可自动执行目录包构建并校验 `.app / Info.plist / app.asar` 是否齐全
- `npm run release:manifest` 可为当前 `release/` 目录生成发布物清单与校验信息
- `npm run dist:desktop:ready` 已实测成功产出 `zip / dmg / blockmap / latest-mac.yml`，并会在构建完成后自动刷新 `release-manifest.json`

## 2. 现有脚本

### 2.1 构建目录包

```bash
npm run package:desktop
```

该命令会：

1. 先执行 `npm run build`
2. 再通过 `electron-builder --dir` 生成目录形态的桌面包

补充说明：

- 该命令保留正常的 Electron Builder 默认行为
- 如果本机存在可用签名证书，可能会进入签名流程
- 适合真正准备产出目录包时使用

### 2.2 构建可分发包

```bash
npm run dist:desktop
```

该命令会：

1. 先执行 `npm run build`
2. 再通过 `electron-builder` 生成 macOS 可分发产物

### 2.3 发布前自动验收

```bash
npm run preflight:release
```

该命令会顺序执行：

1. `npm run build`
2. `npm test`
3. `npm run smoke:gateway`
4. `npm run smoke:desktop-package`

适合在准备打包或提交发布物前做一次完整预检。

### 2.4 生成发布物清单

```bash
npm run release:manifest
```

该命令会扫描当前 `release/` 目录，并生成：

- `release/release-manifest.json`

当前清单会记录：

- `.app` 目录包的文件数量与总体积
- 其他发布物文件的相对路径、大小与 `sha256`
- 清单生成时间

适合在你完成目录包、`zip` 或 `dmg` 构建后，做一次本地留档与版本核对。

### 2.5 一键生成可核对发布物

```bash
npm run package:desktop:ready
```

或：

```bash
npm run dist:desktop:ready
```

它们会在通过预检后，继续构建桌面产物并自动生成 `release-manifest.json`。

补充说明：

- `smoke:desktop-package` 会关闭真实开发证书自动发现，优先走不依赖本机证书权限的目录包验收
- 它的目标是验证桌面包能否成功产出，以及 `.app / Info.plist / app.asar` 是否齐全
- 在 macOS 上它通常会退到 `ad-hoc signature`，但不会替代最终的正式签名 / 公证发布流程
- 它的意义是避免预检阶段被真实证书签名、公证或本机权限问题拖住

## 3. 打包配置

打包配置文件位于：

- [electron-builder.yml](/Users/henery/code/local-ai-gateway/electron-builder.yml)

当前约定：

- 输出目录：`release/`
- 产品名：`Local AI Gateway`
- 平台：macOS
- 目标格式：`zip`、`dmg`
- 发布物清单：`release/release-manifest.json`

当前实际状态：

- 目录包已验证可生成
- 目录包 smoke 已自动化，可校验 `.app`、`Info.plist` 与 `app.asar`
- `zip / dmg` 可分发产物已完成一次真实构建验证
- notarization 尚未接入，因此当前会跳过公证

## 4. 当前限制

当前脚手架仍属于基础版，后续建议继续补齐：

- 应用图标
- 代码签名
- notarization
- 版本号与产物命名策略
- 发布清单与校验信息
- 安装包首次启动引导与升级说明

说明：

- “发布清单与校验信息”现已具备基础版能力：`release-manifest.json`
- 后续若进入正式分发阶段，再考虑补更严格的版本策略与签名 / 公证链路

## 5. 建议发布前检查

在执行打包前，建议至少先运行：

```bash
npm run build
npm test
npm run smoke:gateway
npm run smoke:desktop-package
```
